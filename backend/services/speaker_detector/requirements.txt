# speaker_detector — active speaker detection + A/V sync verification  [GPU]
#
# Class → dependency map:
#   MfccExtractor    (audio_features.py)   scipy (wavfile) + python_speech_features
#   TrackCropReader  (track_crops.py)      opencv-python, numpy
#   TalkNetASD       (active_speaker.py)   torch + the external TalkNet-ASD repo:
#                                          git clone github.com/TaoRuijie/TalkNet-ASD
#                                          (fără pip install — pe sys.path)
#                                          weights: models/talknet_asd.pth
#   SpeakerSelector  (speaker_selector.py) pure logic over the above
#   SyncNetModel     (syncnet_model.py)    torch (architecture only)
#   SyncNetVerifier  (sync_verifier.py)    torch; weights: models/syncnet_v2.pth
#   (fallback paths use scipy.ndimage.zoom)

# CUDA install (RTX 3050 Ti):
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
torch>=2.1
python_speech_features>=0.6
scipy>=1.11
opencv-python>=4.8
numpy>=1.24
loguru>=0.7
