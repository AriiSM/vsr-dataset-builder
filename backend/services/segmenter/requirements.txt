# segmenter — sentence & speaker segmentation  [GPU]
#
# Class → dependency map:
#   SileroVAD          (voice_activity.py)    torch (hub model), torchaudio (resample), soundfile
#   WhisperTranscriber (transcription.py)     whisperx (+ its faster-whisper backend), numpy
#   SpeakerDiarizer    (diarization.py)       pyannote.audio — GATED model:
#                                             free HF account + accept terms at
#                                             hf.co/pyannote/speaker-diarization-3.1,
#                                             token in config (segmentation.diarization.hf_token)
#   SentenceSegmenter  (sentence_segmenter.py) stdlib only
#   ClipCutter         (clip_cutter.py)       imageio-ffmpeg (bundled ffmpeg binary)
#   ClipManifest       (clip_manifest.py)     stdlib only
#   VADSplitter        (vad_splitter.py)      opencv-python (media duration on legacy recovery)

# CUDA install (RTX 3050 Ti):
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu128
torch>=2.1
torchaudio>=2.1

whisperx>=3.1
pyannote.audio>=3.1    # optional at runtime — only loaded when diarization.enabled

soundfile>=0.12
imageio-ffmpeg>=0.4
numpy>=1.24
opencv-python>=4.8
loguru>=0.7
